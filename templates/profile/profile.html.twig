{% extends 'base.html.twig' %}

{% block title %}Tu Perfil - ClimbUp{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('styles/pages/profile/edit_profile.css') }}">
{% endblock %}

{% block body %}
    <div class="profile-container">
        <h1 class="profile-title">Tu Perfil</h1>

        <div class="profile-card">
            {{ form_start(form) }}
            <div class="form-group">
                {{ form_label(form.name, 'Nombre', {'label_attr': {'class': 'form-label'}}) }}
                <i class="fas fa-user input-icon"></i>
                {{ form_widget(form.name, {
                    'attr': {
                        'class': 'form-control',
                        'placeholder': 'Tu nombre'
                    }
                }) }}
            </div>

            <div class="form-group">
                {{ form_label(form.email, 'Correo Electrónico', {'label_attr': {'class': 'form-label'}}) }}
                <i class="fas fa-envelope input-icon"></i>
                {{ form_widget(form.email, {
                    'attr': {
                        'class': 'form-control',
                        'readonly': 'readonly'
                    }
                }) }}
            </div>

            <div class="form-group">
                {{ form_label(form.plainPassword, 'Nueva Contraseña (opcional)', {'label_attr': {'class': 'form-label'}}) }}
                <i class="fas fa-lock input-icon"></i>
                {{ form_widget(form.plainPassword, {
                    'attr': {
                        'class': 'form-control',
                        'placeholder': 'Deja en blanco para no cambiar'
                    }
                }) }}
            </div>

            <button type="submit" class="btn btn-profile">
                <i class="fas fa-save me-2"></i>
                Guardar cambios
            </button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
